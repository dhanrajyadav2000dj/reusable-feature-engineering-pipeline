# Validation Report

Run `python run_feature_pipeline.py` after adding Kaggle data to generate the full validation evidence.

Expected evidence includes raw shapes, train/test schema comparison, missing summaries, target separation, feature inclusion/exclusion, encoded feature counts, final shapes, final missing checks, numeric-only checks, train/test column alignment, unknown-category handling, `MSSubClass` categorical handling, `GrLivArea` outlier handling, year/date consistency, and derived feature range checks.
